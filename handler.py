from asyncio import log
import json



def handle(event,handle):
        s = 'Daniel'
        n = len(s)
        for i in range(log,n):
            if s[i] <= s[5]:
                return{
                    
                }
            